# Co-Found Frontend (Vite + React)

Local dev:

```bash
# install
npm install

# dev server
npm run dev
```

Notes:

- Backend baseURL is set to `https://cofound.dpdns.org/api/v1` in `src/services/api.js`.
- First step implemented: Axios instance and `/login` page.
